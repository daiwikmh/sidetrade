{
  "name": "SIDETRADE-dapp",
  "version": "1.0.0",
  "description": "SideShift API + Telegram DApp Backend",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install && cd ..",
    "test:api": "curl http://localhost:3001/api/health",
    "deploy:backend": "echo 'See DEPLOYMENT.md for deployment instructions'",
    "deploy:frontend": "cd client && npm run build && echo 'Deploy client/dist to your hosting provider'"
  },
  "keywords": ["telegram", "sideshift", "dapp", "crypto", "swap"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "cors": "^2.8.5"
  }
}
